using UnityEngine;

//attached to an empty GO. No collider

//might just need **interface** since changing position could just be animated? or maybe this script just plays the animator???
//nah **player script could trigger this object to animate**. 
public class ProjectileTarget : MonoBehaviour
{
    
    void Update()
    {
        //make the transform change depending on state.
        //if walking, move w/ walk cycle, etc.
        
    }
}
